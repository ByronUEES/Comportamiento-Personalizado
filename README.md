# Comportamiento-Personalizado
cd /mnt/data/unsupervised_user_segmentation
git init
git add .
git commit -m "Entrega: segmentación no supervisada (KMeans, DBSCAN, PCA, t-SNE)"
git branch -M main
git remote add origin https://github.com/TU_USUARIO/unsupervised-user-segmentation.git
git push -u origin main
